<<<<<<< SEARCH
	// Promote the pipeline.
	promoteBody := map[string]any{
		"pipelineId": pipeID,
		"approvedBy": "admin",
	}
	promoteResp := authedPost(t, srv.URL+"/api/pipelines/promote", token, promoteBody)
=======
	// Manually set it to STAGING using database seeder concept/internals if possible, or wait.
	// Since dashboard keeps state in memory `s.pipelines`, let's check if there is an endpoint to update it or we just add it to test.
	// We can try calling promote and expect failure if not staging, or find a way to set it to staging. Let's look at pipeline endpoints.
	promoteBody := map[string]any{
		"pipelineId": pipeID,
		"approvedBy": "admin",
	}
	promoteResp := authedPost(t, srv.URL+"/api/pipelines/promote", token, promoteBody)
>>>>>>> REPLACE
