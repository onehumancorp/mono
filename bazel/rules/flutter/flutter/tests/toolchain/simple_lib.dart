/// A simple Dart library for testing
library simple_lib;

/// A simple class for testing
class SimpleClass {
  final String name;
  
  SimpleClass(this.name);
  
  String greet() {
    return 'Hello, $name!';
  }
}