/// A pure Dart library for mathematical utilities
library dart_package;

import 'package:fixnum/fixnum.dart';

/// Add two integers
int add(int a, int b) {
  return a + b;
}

/// Multiply two 64-bit integers
Int64 multiply64(Int64 a, Int64 b) {
  return a * b;
}