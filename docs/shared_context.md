# Shared Context

This file maintains the synchronization state between the documentation and the source codebase.

<div style="backdrop-filter: blur(20px) saturate(200%); background: rgba(0, 0, 0, 0.4); padding: 20px; border-radius: 12px; border: 1px solid rgba(255,255,255,0.1); color: white; font-family: 'Outfit', 'Inter', sans-serif;">
<strong>Developer Insight:</strong> All codebase source files are located under `srcs/` (e.g., `srcs/orchestration`, `srcs/frontend`). Any historical references to `src/` in documentation must be migrated to `srcs/` to ensure Bazel build path consistency.
</div>
